@extends('layouts.app')
@section('content')
<div class="flex flex-col gap-6"><section><p class="text-sm font-medium text-indigo-600">Risk report</p><h1 class="mt-1 text-3xl font-bold">Duplicate Shipping Addresses</h1><p class="mt-2 text-slate-500">Find different customer emails shipping to the same normalized address.</p></section>
<form class="grid gap-4 rounded-xl border p-5 sm:grid-cols-3" method="POST" action="{{ route('reports.duplicate-addresses.store') }}">@csrf @foreach(['start_date'=>['From',$startDate],'end_date'=>['To',$endDate]] as $field=>[$label,$value])<div><label for="{{ $field }}">{{ $label }}</label><input class="mt-2 w-full rounded-lg border px-3 py-2" id="{{ $field }}" name="{{ $field }}" type="date" value="{{ old($field,$value) }}">@error($field)<p class="text-sm text-red-600">{{ $message }}</p>@enderror</div>@endforeach<div class="flex items-end"><button class="rounded-lg bg-indigo-600 px-5 py-2.5 font-semibold text-white">Run report</button></div></form>
@if($configurationError)<div class="rounded-xl bg-amber-50 p-4 text-amber-800">Shopify credentials are incomplete for the active store.</div>@endif @if($reportFailed)<div class="rounded-xl bg-red-50 p-4 text-red-800">The report could not be completed. Check Shopify and try again.</div>@endif
@if($result)<h2 class="text-2xl font-bold">{{ $result->scanned }} scanned · {{ count($result->rows) }} shared addresses</h2>@if($result->truncated)<div class="rounded-xl bg-amber-50 p-4 text-amber-800">Results are incomplete: orders truncated after {{ $result->pages }} pages.</div>@endif<div class="overflow-x-auto rounded-xl border"><table class="min-w-full text-left text-sm"><thead><tr><th class="p-3">Address</th><th>Name</th><th>Emails</th><th>Orders</th><th>Details</th></tr></thead><tbody>@forelse($result->rows as $row)<tr class="align-top"><td class="p-3">{{ $row['address_line'] }}</td><td>{{ $row['address_name'] ?: '—' }}</td><td>{{ implode(', ', $row['emails']) }}</td><td>{{ $row['order_count'] }}</td><td>@foreach($row['orders'] as $order)<div>@if($order['shopify_id'])<a class="text-indigo-600" href="https://{{ $activeStore->shopify_store }}.myshopify.com/admin/orders/{{ $order['shopify_id'] }}">{{ $order['order_number'] }}</a>@else{{ $order['order_number'] }}@endif · {{ $order['email'] }}</div>@endforeach</td></tr>@empty<tr><td class="p-6 text-center" colspan="5">No duplicate shipping addresses.</td></tr>@endforelse</tbody></table></div>@endif</div>
@endsection
