@extends('layouts.app')
@section('content')
<div class="flex flex-col gap-6"><section><p class="text-sm font-medium text-indigo-600">Audit report</p><h1 class="text-3xl font-bold">Billing ≠ shipping country</h1><p class="mt-2 text-slate-500">Manual review queue for paid orders whose billing and shipping countries differ.</p></section>
<form class="grid gap-4 rounded-xl border bg-white p-5 sm:grid-cols-3 dark:border-slate-800 dark:bg-slate-900" method="POST" action="{{ route('reports.country-mismatch.store') }}">@csrf @foreach(['start_date' => ['From', $startDate], 'end_date' => ['To', $endDate]] as $field => [$label, $value])<div><label for="{{ $field }}">{{ $label }}</label><input class="mt-2 w-full rounded-lg border px-3 py-2 dark:bg-slate-950" id="{{ $field }}" name="{{ $field }}" type="date" value="{{ old($field, $value) }}">@error($field)<p class="text-sm text-red-600">{{ $message }}</p>@enderror</div>@endforeach<div class="flex items-end"><button class="rounded-lg bg-indigo-600 px-5 py-2.5 font-semibold text-white">Run report</button></div></form>
@if($reportFailed)<div class="rounded-xl border border-red-200 bg-red-50 p-5 text-red-800">The report could not be completed. Check Shopify and try again.</div>@endif
@if($result)<section class="flex flex-col gap-4"><h2 class="text-2xl font-bold">{{ $result->scanned }} scanned · {{ count($result->rows) }} mismatches</h2>@if($result->skippedMissingCountry)<p class="text-sm text-slate-500">{{ $result->skippedMissingCountry }} skipped because an ISO country code was missing.</p>@endif @if($result->truncated)<div class="rounded-xl border border-amber-200 bg-amber-50 p-4 text-amber-800">Results were truncated after {{ $result->pages }} pages.</div>@endif
@forelse($result->rows as $row)<article class="rounded-xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><div class="flex justify-between gap-4"><div>@if($row['id'])<a class="font-semibold text-indigo-600" href="https://{{ $activeStore->shopify_store }}.myshopify.com/admin/orders/{{ $row['id'] }}" target="_blank" rel="noopener noreferrer">{{ $row['number'] }}</a>@else<strong>{{ $row['number'] }}</strong>@endif<p class="text-sm text-slate-500">{{ $row['created_at'] }} · {{ $row['email'] ?: 'No email' }}</p></div><strong>{{ number_format($row['total'], 2) }} {{ $row['currency'] }}</strong></div><div class="mt-3 flex gap-3"><span>Billing: {{ $row['billing_country'] }}@if($row['billing_name']) · {{ $row['billing_name'] }}@endif</span><span>Shipping: {{ $row['shipping_country'] }}</span><span>{{ $row['financial'] }} · {{ $row['fulfillment'] ?: 'unfulfilled' }}</span></div><a class="mt-3 inline-flex text-sm text-indigo-600" href="{{ route('orders.spot-check', ['prefill' => ltrim($row['number'], '#')]) }}">Open in spot-check</a></article>@empty<div class="rounded-xl border bg-white p-5 text-slate-500">No billing and shipping country mismatches were found.</div>@endforelse</section>@endif</div>
@endsection
