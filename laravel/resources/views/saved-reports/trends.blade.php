@extends('layouts.app')
@section('content')
<div class="flex flex-col gap-6"><section><h1 class="text-3xl font-bold">Audit Trends</h1><p class="mt-2 text-slate-500">Daily missing-order totals from saved Run Audit snapshots.</p></section><form class="grid gap-4 rounded-xl border p-5 sm:grid-cols-3" method="GET">@foreach(['start_date'=>['From',$startDate],'end_date'=>['To',$endDate]] as $field=>[$label,$value])<div><label for="{{ $field }}">{{ $label }}</label><input class="w-full rounded-lg border px-3 py-2" id="{{ $field }}" name="{{ $field }}" type="date" value="{{ old($field,$value) }}">@error($field)<p class="text-red-600">{{ $message }}</p>@enderror</div>@endforeach<button class="rounded-lg bg-indigo-600 px-5 py-2 text-white">Apply</button></form><div class="overflow-x-auto rounded-xl border"><table class="min-w-full text-left"><thead><tr><th class="p-3">Date</th><th>Missing</th><th>Change</th><th class="w-1/2">Trend</th></tr></thead><tbody>@forelse($rows as $row)<tr><td class="p-3"><a class="text-indigo-600" href="{{ route('saved-reports.show',$row['id']) }}">{{ $row['date'] }}</a></td><td>{{ $row['missing'] }}</td><td>@if($row['delta'] === null)—@elseif($row['delta'] > 0)+{{ $row['delta'] }}@else{{ $row['delta'] }}@endif</td><td><div class="h-3 rounded bg-indigo-500" style="width: {{ $maximum > 0 ? round($row['missing'] / $maximum * 100) : 0 }}%"></div></td></tr>@empty<tr><td class="p-6 text-center" colspan="4">No saved audit data for this period.</td></tr>@endforelse</tbody></table></div></div>
@endsection
