# Laravel rewrite — legacy test audit

Последно обновяване: **2026-09-11**. Пълната история на затворените редове и четирите поправени production bug-а е в git log-а на `docs/`; последният slice затвори `SecurityTest.php`, `ScanRunnerTest.php`, `ViewHelpersTest.php`, `PageLoaderTest.php` и разшири `RouteAuthorizationTest.php` да покрива `ignored-orders.*`/`jobs.*`/`print-queue.*`/`orders.push.*`/`orders.note.*`, не само `reports.*`/`admin.*`. `AuthTest.php` остава частично отворен заради реален security gap — виж реда му по-долу.

Този документ е отделният checklist за тестова parity. Feature статусът се следи
в [Laravel rewrite плана](laravel-rewrite.md), а тук се затваря всеки legacy test
файл. Един файл се отбелязва като готов само след method-level сверка: всеки стар
contract има Laravel тест, по-силен еквивалент или записана причина за отпадане.

## Състояние

| Статус | Файлове | Дял от 115 |
|---|---:|---:|
| Готови | 106 | 92.2% |
| Частично покрити | 6 | 5.2% |
| Непочнати | 3 | 2.6% |
| **Оставащи за одит** | **9** | **7.8%** |

Legacy baseline: **115 файла · 1,528 теста · 3,659 assertions**. Laravel
baseline след последния slice: **660 теста · 2,833 assertions**. Броят assertions
е ориентир; критерият е поведенческо покритие.

За всеки checkbox проверяваме business decisions, boundary интеграцията,
authorization и store isolation, validation, escaping, pagination/truncation,
malformed payloads и atomic failure. Не копираме тест, който проверява изцяло
премахнат legacy implementation detail; записваме защо Laravel contract-ът го
заменя.

## Частично покрити — method-level одит остава

| Готово | Legacy файл | Тестове | Какво проверява | Какво остава |
|---|---|---:|---|---|
| [x] | `AllViewsSmokeTest.php` | 1 | Replaced by automatic traversal of every parameterless GET screen backed by an application controller, with authenticated admin/store context and safe webhook boundary |
| [x] | `AuthPermissionSnapshotTest.php` | 2 | Replaced by automatic completeness checks for every report/admin route plus a runtime viewer-denial assertion; this also closed missing POST/export report gates |
| [ ] | `AuthTest.php` | 40 | Пароли, users, CSRF и роли; permission матрицата е потвърдена и разширена — `RouteAuthorizationTest.php` вече проверява и `ignored-orders.*`/`jobs.*`/`print-queue.*`/`orders.push.*`/`orders.note.*`, не само `reports.*`/`admin.*` | **Real gap, флагнат за потребителя**: legacy бан-ва IP за 1 седмица след 3 неуспешни опита в 1-часов прозорец, с admin-видим banned-IP списък и unban; Laravel има само `RateLimiter::for('login', Limit::perMinute(5))` — reset-ва се за 1 минута, без persistent ban, без admin visibility, без unban. Това е реална security posture разлика, не test gap — изисква продуктово решение (build proper lockout, or decide the OAuth+rate-limit combo is sufficient) |
| [x] | `FraudComplianceChecksTest.php` | 22 | Country mismatch, high value/no phone и email checker rules са покрити с analyzer unit и HTTP feature тестове; non-ISO country names умишлено се третират като липсващи вместо да създават false positives |
| [x] | `GraphQL/OrderEventLookupTest.php` | 3 | Shopify client integration tests покриват normalized event lookup, cursor pagination/newest-first order и missing order; добавени са malformed shape, cursor и invalid-ID guards |
| [ ] | `GraphQL/OrderNormalizerTest.php` | 39 | Core order fields (id, dates, financial/fulfillment status, price) са покрити 1:1 в `ShopifyOrderNormalizer` | Confirmed absent from the shared normalizer and not present anywhere else in the Laravel app (`grep`-verified): `total_tax`, `customer_tax_exempt`/`customer_email_consent`/`customer_sms_consent`, `discount_codes`, `note_attributes`, `client_ip`, `test`, `customer_journey`, `source_name`, `app_name`, `current_total_price`, `edited`, `payment_gateway_names`, `po_number`, `confirmation_number`, `status_page_url`, `customer_locale`. Tax/consent/discount/client_ip fields are legitimately handled inline by their own report-specific candidate methods instead (matches the established no-shared-normalizer pattern). The attribution/journey/source/support fields have no Laravel consumer at all yet — they tie to `OrderInsightPageLoaderTest.php`/`OrderTimelineTest.php`, both still open; close all three together |
| [x] | `HttpAuthEndpointTest.php` | 1 | Laravel HTTP feature tests cover login/logout, Google redirect/callback failures, session regeneration, throttling, CSP and authenticated route boundaries |
| [ ] | `OrderInsightPageLoaderTest.php` | 12 | Compare, timeline и допълнителни order insights | Непренесените insight branches и failure states |
| [ ] | `OrderTimelineTest.php` | 26 | Timeline events, ordering, labels и risk signals | Explicit mapping на всички 26 метода |
| [ ] | `OrderPolicyPageLoaderTest.php` | 22 | Policy-report inputs, wiring, configuration и error states | Discount Abuse, Same IP, Tag Policy, Duplicate Shipping Addresses, Note Flags и Order Edit paths са покрити; останалите policy reports чакат method-level сверка |
| [ ] | `ProductInventoryPageLoaderTest.php` | 32 | Wiring за catalogue/inventory report страниците | Оставащите catalogue workflows и финална method-level сверка |
| [x] | `StoresTest.php` | 7 | File-backed stores are replaced by DB stores, user pivots and active-store middleware; first-store fallback, switching, inaccessible-store rejection and session persistence are covered |
| [x] | `ViewSmokeTest.php` | 6 | Fraud Risk, Same IP and Disputes empty/populated rendering is covered by the automatic GET smoke test plus their feature success, escaping and safe-failure tests |

## Непочнати — application и infrastructure

| Готово | Legacy файл | Тестове | Какво проверява / Laravel цел |
|---|---|---:|---|
| [x] | `AtomicFileTest.php` | 8 | Replaced: operational state uses database writes/transactions and validated JSON casts; Laravel storage owns atomic filesystem writes where files remain |
| [x] | `AuditSnapshotTest.php` | 9 | Save/load/history/overwrite на audit snapshots → store-scoped DB snapshots and Saved Reports views |
| [x] | `AuditTest.php` | 3 | Success/error execution logging → persisted Run Audit summaries and safe failure records |
| [x] | `AutoloadCoverageTest.php` | 1 | Automatic PSR-4 check loads every PHP symbol under `app/` through Composer |
| [x] | `ConfigValidatorTest.php` | 35 | Replaced by runtime Laravel application/security, DB store credentials, order-type/tag-policy contracts and admin-only rendering |
| [x] | `DateRangeTest.php` | 10 | Отделните GET/POST routes премахват input precedence двусмислието; Form Requests покриват строг ISO формат и ред на датите, а Carbon покрива calendar arithmetic и седемдневното ShipStation разширение |
| [x] | `DocsGeneratorTest.php` | 1 | Feature tracker count is executable and must remain exactly 72; routes are the Laravel source of truth instead of generated legacy tool docs |
| [x] | `IgnoreListTest.php` | 16 | Ignore CRUD, normalization, expiry и persistence → ignore-list model/repository |
| [x] | `JsonFileLockTest.php` | 6 | Replaced by database transactions/unique constraints and native Laravel cache locks; no shared JSON state remains in Laravel workflows |
| [x] | `PrintQueueTest.php` | 7 | Queue persistence, ordering и removal → DB-backed store-scoped packing-slip queue |
| [x] | `PushLogTest.php` | 3 | Push history append/order/limit → store-scoped DB push log |
| [x] | `ReportRegistryTest.php` | 7 | Named routes are the report registry; executable checks enforce unique names and a submit route for every report screen |
| [x] | `RunLogTest.php` | 3 | Run history append/order/limit → DB run records |

## Непочнати — order, fulfillment и logistics workflows

| Готово | Legacy файл | Тестове | Какво проверява / Laravel цел |
|---|---|---:|---|
| [x] | `CustomerLTVPageLoaderTest.php` | 28 | Revenue/customer cohorts, cancellation, identity normalization, retention and range wiring |
| [ ] | `FulfillmentIssuePageLoaderTest.php` | 36 | Loader contracts за fulfillment exceptions, filters, dates, credentials и failures |
| [ ] | `OrderAnomalyPageLoaderTest.php` | 20 | Fraud/anomaly page dispatch, ranges, credentials, results и failures |
| [ ] | `SimpleScanPageLoaderTest.php` | 19 | Shared tag/tax/returns/email report loader, validation and notifications | Email wiring/credentials са покрити; returns и notification branches остават |

## Непочнати — Shopify GraphQL contracts

| Готово | Legacy файл | Тестове | Какво проверява / Laravel цел |
|---|---|---:|---|
| [x] | `GraphQL/AdminLookupsTest.php` | 3 | Legacy facade е заменен с директен Shopify gateway contract; order, metafield и customer lookups са покрити на client и HTTP controller границите |
| [x] | `GraphQL/CustomDataLookupsTest.php` | 6 | Metafield search, counts, samples, dedupe и query escaping |
| [x] | `GraphQL/CustomerOrderInsightsTest.php` | 6 | Customer spend, identity selection, email normalization and defaults |
| [x] | `GraphQL/DuplicateOrderInsightsTest.php` | 7 | Duplicate window boundary, amount/email matching and scanned count |
| [x] | `GraphQL/MetafieldNormalizerTest.php` | 6 | Types, JSON, references and malformed metafield values |
| [x] | `GraphQL/OrderInsightsTest.php` | 2 | Legacy facade е премахнат; tag search и duplicate-order insights се изпълняват директно през Shopify gateway и са покрити с integration, analyzer и controller тестове |

## Напълно сверени

- [x] `GraphQL/OrderDirectLookupTest.php` — `#`-stripping, normalization, empty-result and batch dedup/keying-with-misses are covered by the Shopify gateway test; legacy `getOrder`-by-GID is consolidated into the same name-based `findByOrderNumber` query (Laravel always resolves orders by name), and the 60s cache pass-through is intentionally dropped for fresh reads.
- [x] `JobQueueTest.php` — native `jobs`/`failed_jobs` visibility and retry/forget are covered by the admin queue controller test; `RunAuditJob::handle()` store resolution and delegation to `RunAudit` (success and missing-store failure) are covered directly; the queue worker's own failure/retry mechanics are Laravel framework behavior, not app logic.
- [x] `UserActionLogTest.php` — newest-first history and store-change/credential-rotation logging are covered by the DB-backed admin Action Log (Spatie Activitylog); scheduled retention (`activitylog:clean`) is now directly asserted; the legacy JSON→SQLite storage-driver import is a dual-storage migration artifact with no Laravel equivalent (the Action Log is DB-native from day one, same reasoning as `AtomicFileTest.php`/`JsonFileLockTest.php`).
- [x] `AuthViewsTest.php` — login mode toggling, incomplete-config error, and escaping are covered by the login feature tests; the dedicated access-denied page is replaced by an inline session-flashed error on the login form, and each denial reason (unknown user, disallowed domain, google-id conflict) now asserts its exact displayed message instead of a generic "has errors" check; per-deployment branding (logo/app name/background image) and the localhost dev-login bypass are intentionally dropped in the rewrite (single fixed brand, no auth-bypass route).
- [x] `ReporterTest.php` — this legacy class only has two public methods, both CLI-era: `saveReports()` (local CSV/TXT files) and `printSummary()` (terminal echo). Neither has a Laravel equivalent — the multi-tenant web app persists the same `run_audit` report to `audit_snapshots`/`run_logs` DB tables and renders it in the Admin UI (`RunAuditControllerTest.php`) instead of writing local files or echoing to a terminal; the "spot-check" section maps to the separate, already-closed `SearchLookupPageLoaderTest.php` (`OrderBatchLookupController`). Added the one missing case: the zero-missing "OK" success state and its `run_logs` status. There is no JSON output anywhere in the legacy class or its tests — that part of the old audit note was inaccurate.
- [x] `RiskScorerTest.php` — all 8 default signal weights match legacy exactly, verified via `OrderRiskScorerTest.php`'s per-signal data provider (each signal's exact weight), the four threshold-boundary pins (20/low, 25/medium, 50/medium, 55/high) and multi-signal accumulation in legacy evaluation order — a superset of the legacy 33 tests. The `data/risk_weights.json` custom-weight override is intentionally not ported: it was never shipped with an actual `.example` file even in the legacy app, has no dedicated management UI, and is only mentioned in a help blurb — a YAGNI candidate, not a test gap.
- [x] `GraphQL/IdsTest.php` — there is no shared reusable ID utility class in Laravel; the legacy `orderGid`/`legacyId` contract is duplicated as small private helpers on `ShopifyAdminClient`/`ShopifyOrderNormalizer` scoped to each caller. Numeric-ID-to-GID conversion, GID-passthrough (the actual code path `LoadOrderTimeline` uses), invalid-ID rejection, and GID-fallback legacy-ID extraction (line items, transactions with no `legacyResourceId`) are all covered through the real order/event normalization tests; a dedicated GID-passthrough assertion was added to `getOrderEvents`. Query-string-in-GID and non-numeric-`legacyResourceId` are theoretical shapes Shopify never actually returns and are not worth separate coverage.
- [x] `GraphQL/DisputeLookupTest.php` — open-status filter, normalization (including reason, network reason code, amount, currency and order name), cursor pagination and disputes without an order.
- [x] `GraphQL/OrderArchiveTest.php` — inclusive all-status date range, normalized multi-page results and cursor forwarding are covered by the customer LTV gateway; legacy cache pass-through is intentionally replaced by fresh report reads.
- [x] `SlackRulesTest.php` — audit/scan thresholds, defaults, mention normalization, DB persistence and shared delivery wiring.
- [x] `SlackNotifierTest.php` — queue-ready webhook channel, admin-only delivery diagnostic, trusted endpoint validation and credential-free test payload were already covered; added direct unit coverage for `AuditSlackNotification`/`ScanSlackNotification` mentions formatting, both the multi-ID `<@id>` prefix branch and the no-mentions branch. The legacy rich block-kit payload (per-metric fields, up-to-10 missing-orders list, duration field) is intentionally replaced by a single-line Slack message — the counts it summarized (`missing`/`rows`/`period`) are still asserted, and the scannable-feed layout is no longer load-bearing once delivery is queued rather than a CLI print. `notifyAuditSafely`'s catch-log-return(false) pattern has no direct Laravel equivalent because delivery is `ShouldQueue` (asserted here): a failed webhook is retried and logged by the queue worker's own `tries`/`failed_jobs` mechanism instead of app-level try/catch, the same reasoning already recorded for `JobQueueTest.php`.
- [x] `GraphQL/OrderFetcherTest.php` — id dedup/empty-id skip, node-filter application and event normalization are exercised implicitly through every one of the ~44 `Shopify*CandidatesTest.php`/`ShopifyAdminClientTest.php` fetches, which all go through the same inline pagination/parse mechanics on `ShopifyAdminClient` (there is no separate `OrderFetcher` class — same "no shared reusable class" reasoning as `GraphQL/IdsTest.php`). The 300s/900s `remember()` cache pass-through is intentionally dropped, matching the `OrderDirectLookupTest`/`OrderArchiveTest` fresh-reads precedent.
- [x] `GraphQL/OrderEventAuditsTest.php` — `fetchEditedOrders`/`groupEditEventsByOrder`/`buildEditedOrderRows` (latest-timestamp-wins, 4-unique-message cap, `diff_mins` clamp, newest-first sort) are covered 1:1 by `OrderEditAnalyzer`/`OrderEditAnalyzerTest.php`; `fetchOrdersWithAddressChanges`/`changedAddressOrderIds` and `fetchPostShipAddressChanges` (earliest-fulfillment selection, changed-before-ship exclusion) are covered 1:1 by `AddressChangeAnalyzer`/`AddressChangeAnalyzerTest.php` and `PostShipAddressChangeAnalyzerTest.php`. The GraphQL fetch/batch wiring around them is exercised by `ShopifyOrderEditCandidatesTest.php`/`ShopifyAddressChangeCandidatesTest.php`.
- [x] `GraphQL/OrderAuditsTest.php` — pure backward-compatible facade with no logic of its own (delegates every method straight to `OrderQueryAudits`/`OrderEventAudits`); both delegates are closed below.
- [x] `GraphQL/OrderQueryAuditsTest.php` — all 18 query-building methods are one-to-one with an already-closed, report-specific `Shopify*CandidatesTest.php` (e.g. `ShopifyPolicyAuditCandidatesTest.php` for tax/consent, `ShopifyAddressCheckCandidatesTest.php`, `ShopifyFraudRiskCandidatesTest.php`, `ShopifySameIpCandidatesTest.php`, `ShopifyDiscountAbuseCandidatesTest.php`, `ShopifyTagPolicyTest.php`, `ShopifyCountryMismatchCandidatesTest.php`, `ShopifyHighValueCandidatesTest.php`, `ShopifyPartialFulfillmentCandidatesTest.php`, `ShopifyFulfilledItemCandidatesTest.php`, `ShopifyShippingMarginCandidatesTest.php`, `ShopifyRepeatRefundCandidatesTest.php`, `ShopifyNoteFlagCandidatesTest.php`, `ShopifyDuplicateOrderCandidatesTest.php`), each asserting the exact `Http::assertSent` query-filter string, not just delegation. **Fixed a real discrepancy**: legacy `fetchPartiallyFulfilledOrders` filters `-financial_status:refunded` (exclude only fully-refunded orders, per `docs/audit-checks.md`'s "excludes cancelled, fully refunded, and closed orders"); `partialFulfillmentCandidates` (`ShopifyAdminClient.php:1021`) was filtering the stricter `financial_status:paid`, silently dropping partially-refunded-but-partially-fulfilled orders — corrected to `-financial_status:refunded` and `ShopifyPartialFulfillmentCandidatesTest.php` updated to lock in the new filter string.
- [x] `GraphQL/QueryStringsTest.php` — `status:open`, `fulfillment_status:partial`, `-financial_status:refunded` and both date-range boundaries now match `ShopifyPartialFulfillmentCandidatesTest.php` exactly (see the fix recorded under `GraphQL/OrderQueryAuditsTest.php` directly above).
- [x] `GraphQL/OrderHoldLookupTest.php` — on-hold detection is not a per-order `isOnHold`/batched `findOnHoldOrderIds` GraphQL facade in Laravel; `onHoldFulfillmentCandidates` (`ShopifyAdminClient.php:1049`) instead fetches every currently on-hold fulfillment order once per audit run (`fulfillmentOrders(query: "status:on_hold")`, date-filtered client-side) and `RunAudit`/`AuditOrderAnalyzer` skip any missing order whose id is in that set — a single global batch query rather than N per-order/ID-batch lookups, functionally equivalent and covered by `ShopifyOnHoldFulfillmentCandidatesTest.php` plus `AuditOrderAnalyzerTest.php`'s `on_hold` skip-reason case. *(Corrects a wrong finding from the previous audit pass, which mistook the unrelated order-pair-diff display class `ShopifyOrderComparator.php` for the actual `Comparator.php` port — the real port is `AuditOrderAnalyzer.php`, see `ComparatorTest.php` below.)*
- [x] `GraphQL/OrderLookupTest.php` — pure facade; `getOrder`/`findByOrderNumber(s)` delegation is covered by `OrderDirectLookupTest.php`, `getOrderEvents` by `OrderEventLookupTest.php`, and hold delegation by `GraphQL/OrderHoldLookupTest.php` directly above.
- [x] `ComparatorTest.php` — the 73 legacy tests split across six independent decisions, each with its own Laravel home: (1) `normalise`/`buildSSIndex`/`orderNumberKeys`/`buildSSEmailIndex`/`compare` (order-number and email+amount matching, skip-reason priority) → `AuditOrderAnalyzer.php`/`AuditOrderAnalyzerTest.php`; (2) `applyOnHoldSkip` → inlined into the same `analyze()` pass rather than a separate callable-driven post-pass (the "exception propagates, don't silently misreport" safety property is preserved structurally: `RunAudit::handle()` wraps the whole audit, including the on-hold fetch, in one try/catch that marks the run `error` and rethrows); (3) `findDuplicates` → `DuplicateOrderAnalyzer.php`/`DuplicateOrderAnalyzerTest.php`; (4) `classifyOrder`/`findMissingRequired` (config-driven bundle rules) → `OrderTypeClassifier.php`/`config/order-types.php`, tested via `OrderTypeClassifierTest.php` and `BundleCheckAnalyzerTest.php`; (5) `diffShippedItems` → `OrderChannelComparator::compare()['items']`, tested via `OrderChannelComparatorTest.php` and used directly by `ItemMismatchAnalyzer.php`; (6) `shippingLoss` → `ShippingMarginAnalyzer.php`/`ShippingMarginAnalyzerTest.php`. **Fixed two real bugs found during this pass** (both had a matching Laravel test that had locked in the wrong behavior, now corrected with regression tests): `DuplicateOrderAnalyzer` was clustering duplicate orders within a **10-minute** window (`<= 600` seconds) instead of legacy's documented **24-hour** window (`<= 86400`) — a order placed twice inside the same hour by mistake would never have been flagged; and `AuditOrderAnalyzer` indexed each ShipStation order under a single fully-normalized number key, so a compound multi-box ShipStation order number like `100042-B2` (Shopify order `#100042`) never matched and was reported as a false-positive "missing" order — `AuditOrderAnalyzer` now builds the same multi-key index as legacy's `orderNumberKeys()` (full digits-joined key plus each standalone digit run of 4+ digits), with a regression test proving both the compound match and that a short suffix fragment like the "2" in "100042-B2" still can't false-match an unrelated order literally named "2".
- [x] `GraphQL/ProductNormalizerTest.php` — there is no shared `ProductNormalizer` class in Laravel (same reasoning as `GraphQL/IdsTest.php`/`GraphQL/OrderFetcherTest.php`): each catalogue analyzer reads `inventoryItem.tracked`/`inventoryPolicy` inline and is covered by its own closed row (`InventoryForecastTest.php`, `ZombieProductsTest.php`, `InventoryOversellCandidatesTest` under `ProductCatalogueChecksTest.php`). Legacy's `mediaCount.count`-based image-count placeholder array is replaced by reading the real `images.nodes` list (`ProductCompletenessAnalyzer.php`), a stronger check (presence, not just count) already covered by `ProductCatalogueChecksTest.php`. The `seo_title`/`seo_description`/`collection_count`/`published` fields on legacy's `normalizeProduct()` were never covered by the legacy test file either (dead/unverified fields) and are not used by any Laravel catalogue report.
- [x] `LoggerTest.php` — the bespoke PSR-3 file logger (size-based rotation, `{}` interpolation, exception-context append) is replaced end-to-end by Laravel's native `Log` facade/Monolog `daily` channel (`PsrLogMessageProcessor` handles the same `{}` interpolation, the `daily` driver handles rotation) — framework behavior, not app logic, so it needs no bespoke app-level test (same reasoning as `AtomicFileTest.php`/`JsonFileLockTest.php`). The legacy test file's own "redaction" description was inaccurate — there is no redaction logic anywhere in `Logger.php` or its tests, only directory creation, writing, level formatting, interpolation, append and rotation.
- [x] `MetricsEndpointTest.php` — the bespoke token-gated Prometheus text endpoint (`missing_orders_total`/`audit_duration_seconds` gauges read from the run log) is replaced by the admin-only Laravel Pulse dashboard (`PulseDashboardTest.php`: DB-backed storage, `admin/pulse` path, `assertForbidden` for non-admins) — a stronger, framework-native operational-monitoring surface; the specific run-count/duration numbers it exposed are already visible to admins on the Run History/Dashboard pages rather than a separate scrape endpoint, and there is no Prometheus/Grafana scrape config anywhere in this repo to preserve compatibility with.
- [x] `WorkerTest.php` — this is the CLI-era precursor to `RunAudit.php`: `configureDataDirs()`/per-store JSON data directories have no Laravel equivalent (DB-native multi-tenancy from day one, same reasoning as `AtomicFileTest.php`); `resolveStore()`'s single/multi-store env resolution and `assertCredentialsPresent()` are replaced by the DB `Store` model and its pivot-scoped active-store resolution (`StoresTest.php`, closed: "first-store fallback, switching, inaccessible-store rejection"), with each ShipStation-dependent report checking and erroring on missing credentials at its own call site (`RunAuditController`/`ItemMismatchController`/etc., each with its own closed feature test) rather than one shared helper; `buildAuditComparison()`'s on-hold skip, missing-order classification, ignored-orders and snapshot-save are exactly `RunAudit::handle()` + `AuditOrderAnalyzer::analyze()`, covered by `AuditOrderAnalyzerTest.php` and `RunAuditControllerTest.php`/`RunAuditJobTest.php` (`JobQueueTest.php`, closed).
- [x] `DiscordNotifierTest.php` — this channel did not exist in Laravel at all; **built it** rather than just verified it, mirroring the already-closed Slack implementation exactly: `DiscordWebhookChannel` (a small custom notification channel, no new Composer dependency — Discord webhooks are a plain JSON POST), `AuditDiscordNotification`/`ScanDiscordNotification`/`DiscordTestNotification`, a `discord_rules` store column + `resolvedDiscordRules()` (same audit/scan enabled+threshold shape as Slack, minus mentions — legacy Discord never supported mentions either), `DiscordRulesController`/`Request`/view/routes/nav link, wired into `RunAudit`/`RecordRun` alongside the existing Slack dispatch, and a Discord section on the API Health page with its own test-delivery diagnostic (`SendTestDiscord`, mirroring `SendTestSlack`'s trusted-endpoint check). Tests: `DiscordWebhookChannelTest.php` (routed delivery, skipped when unrouted, throws on non-2xx instead of failing silently — the legacy `notifyAuditSafely`/`send`/`assertOk` contract), `Audit`/`ScanDiscordNotificationTest.php` (queueable, message format), `SendTestDiscordTest.php` (mirroring `SendTestSlackTest.php`), `DiscordRulesControllerTest.php`, plus wiring assertions added to `RunAuditControllerTest.php`/`RunLogTest.php`. Same simplification as Slack: legacy's rich colored embed (missing-orders list, duration field) is a single text line — the counts it summarized are unaffected since delivery is queued rather than a CLI print.
- [x] `EmailRulesTest.php` — `Store::resolvedEmailRules()`/`EmailRulesRequest`/`EmailRulesController` already existed and replicate every legacy `EmailRules::normalise()` decision verified line-for-line: unknown tool keys dropped, invalid mode ignored, per-tool threshold floor (`run_audit` allows 0, every other tool clamps to 1 — verified in `Store.php:91`, not just the request validator), include_zero cast, one tool's mode never affecting another's. Added a dedicated regression test (`test_non_audit_tools_clamp_a_zero_threshold_to_one_but_run_audit_may_use_zero`) locking in that clamp, since nothing previously asserted it directly. Invalid email addresses are rejected by `EmailRulesRequest`'s `email:rfc` validation rather than legacy's silent-clear-to-blank — a deliberate, stronger UX (the admin sees why their save didn't apply) consistent with every other Form Request in this app.
- [x] `EmailNotifierTest.php` — `ReportEmailNotification` (immediate) and `ReportDigestNotification` (digest) replace `EmailNotifier`'s audit/scan/digest HTML builders; wired through `RecordRun::handle()`/`reports:email-digest` exactly like the already-closed Slack path. Same simplification as `SlackNotifierTest.php`: a single plain-text line instead of an escaped multipart HTML body with a CSV report attachment — CSV export already exists as an authenticated in-app download on every report page (`ReturnedItemsReportTest.php` et al.), so attaching potentially sensitive order data to an email is a deliberate downgrade in favor of requiring authenticated access, not a gap. SMTP configuration diagnostic (`isConfigured`/`fromEnvironment`) is covered by `SendTestEmail`/`SendTestSlackTest.php`'s already-closed sibling pattern.
- [x] `EmailDigestTest.php` — `EmailDigest::buildSections()`'s tool-selection (digest-mode only, latest run per tool, threshold/include_zero gate, grouped by recipient) is reimplemented directly in the `reports:email-digest` scheduled command (`routes/console.php`), tested by `EmailRulesDeliveryTest.php`. **Fixed a real bug found during this pass**: the command selected each tool's latest run using a rolling `now()->subDay()` window instead of legacy's explicit "ran today" calendar-day check (the legacy docblock is explicit: "the digest only ever reports on today's activity, never stale runs") — a tool that last ran late yesterday (e.g. 23:30) would wrongly reappear in this morning's digest as if it were today's result. Changed the filter to `created_at >= today()` and added `test_digest_excludes_a_stale_run_from_yesterday_even_within_the_last_24_hours` (using `travelTo()` to pin the run to 23:30 yesterday and the command run to 08:00 today) to lock in the calendar-day boundary.
- [x] `CacheTest.php` — the bespoke file cache it tests (`remember`/exclusive locking/corruption recovery/flush/checkpoint pruning/namespacing) has zero Laravel equivalent because it has zero Laravel *usage*: `grep -rn 'Cache::' app/` returns nothing anywhere in the rewrite. Every GraphQL fetcher/report candidate deliberately reads fresh on each request instead (the same "cache pass-through intentionally dropped" reasoning already recorded for `OrderDirectLookupTest.php`, `OrderArchiveTest.php` and `GraphQL/OrderFetcherTest.php`) — there is no caching layer left to test.
- [x] `ShopifyFlowHealthTest.php` — `ApiHealthController::flowHealth()` (per-tool runs/errors aggregation from `RunLog`, healthy/attention summary) covers the same decisions, verified by `ApiHealthControllerTest.php::test_page_summarizes_store_scoped_persisted_flow_history` — two `run_audit` runs (error then ok) prove the "recovered tool shows healthy but keeps its last error" case, and the mixed-tool assertions prove per-tool error/attention counting. Legacy's `never_run`/`total` tri-state (which needs `ToolRegistry::triggerCatalog()`, a static list of every possible tool) has no equivalent — Laravel's dashboard only lists tools that have actually produced a `RunLog` row, not a fixed catalog padded with placeholders for tools that have never run; a deliberate simplification, not a gap.
- [x] `ToolRegistryTest.php` — the `?page=X` SPA-style page registry (titles/groups/hub-sections/allowed-pages/normalize-fallback) has no Laravel equivalent by design: every legacy "page" is a real named route with its own controller and Blade `<title>`, and route existence/authorization/titles are already exercised exhaustively by `AllViewsSmokeTest.php` (every parameterless GET screen renders) and `ReportRegistryTest.php` (unique route names, every report has a submit route) — the same "SPA router replaced by real routes" reasoning already used for `GraphQL/OrderLookupTest.php`-style facades. `triggerCatalog()`, the one piece with actual behavioral value (tool→label/page/area mapping), is likewise gone: `EmailRulesController::edit()` builds its own tool list straight from `RunLog::distinct('tool')` instead of a static catalog.
- [x] `SidebarSettingsTest.php` — intentionally dropped, not ported: it toggles visibility of two sections ("Missing Orders", "Recent Activity") in the legacy collapsible sidebar. Laravel's navigation (`layouts/app.blade.php`) is a flat top-nav with no sidebar-section concept at all, so there is no surface for this preference to attach to; building one would mean inventing new navigation UI rather than porting a test, which is out of scope for parity work.
- [x] `ManageSettingsPageLoaderTest.php` — the `load(string $page, ...)` dispatcher itself has no Laravel equivalent (real routes replace the single switch-dispatched settings hub, same reasoning as `ToolRegistryTest.php`). Every sub-page it dispatched to already has its own closed row: jobs → `JobQueueTest.php`, Slack rules → `SlackRulesTest.php`, API health + flow health → `ApiHealthTest.php`/`ShopifyFlowHealthTest.php` above, email rules → `EmailRulesTest.php`, config check → `ConfigValidatorTest.php`, action log → `UserActionLogTest.php`, webhook health → `WebhookHealthControllerTest.php`, print queue → `PrintQueueTest.php`; connections/notification-rule summary → `SettingsControllerTest.php`. The two pieces of `loadSettings()` with no Laravel equivalent — cache entries/TTL/flush display and the sidebar toggle — are covered by the `CacheTest.php`/`SidebarSettingsTest.php` drops directly above.
- [x] `GraphQL/EventNormalizerTest.php` — `normalizeEvent()` matches `ShopifyOrderEventNormalizer::normalize()` field-for-field (id/admin_graphql_api_id/verb/action/created_at/message/subject_id/subject_type/subject_graphql_api_id/app_title, including the subject-id-fallback and lowercasing rules). **Fixed a real bug found during this pass**: `isAddressChangeEvent()`/`isOrderEditEvent()` had no Laravel equivalent at all — `OrderEditAnalyzer::group()` and `ShopifyAdminClient::orderEditCandidates()` both only checked `verb === 'edit_complete'`, missing legacy's message-substring fallbacks (`'was edited'`, `'item was added'`, `'discount was added'`, `'note was updated'`, `'custom attributes'`, etc.) *and*, more importantly, its explicit address-change exclusion — legacy's docblock is explicit that an `edit_complete` event whose message is actually about a shipping address change must not double-count into both the Order Edit History and Address Changes reports. Added `isAddressChangeEvent()`/`isOrderEditEvent()` to `ShopifyOrderEventNormalizer` (mirroring legacy's `EventNormalizer` exactly) and wired both call sites through it, with a full data-provider test suite (`ShopifyOrderEventNormalizerTest.php`) covering every legacy scenario 1:1, plus a regression test on `OrderEditAnalyzerTest.php` proving the exclusion and the fallback both now work.
- [x] `GraphQL/OrderComponentNormalizerTest.php` — `orderNumberFromName()` is byte-for-byte identical to `ShopifyOrderNormalizer`'s private `orderNumber()`; `normalizeAddress()`/`normalizeLineItem()`/`normalizeFulfillment()` match field-for-field (verified during the `EventNormalizerTest.php`/`OrderNormalizerTest.php` review above). `normalizeShippingLine()`/`normalizeDiscountCode()` and refund line-item detail have no shared Laravel class — same "no shared reusable normalizer" reasoning as `GraphQL/IdsTest.php`/`GraphQL/ProductNormalizerTest.php`: `itemMismatchCandidates()` only needs shipping-line *presence* (its query requests just `id`, not price, so the `no_shipping` skip check never needed a normalized shape), `shippingMarginCandidates()`/discount-reading callers each query and map exactly the fields they need inline, and `refundLineItems` is only ever requested by `repeatRefundCandidates()`, which builds its own richer refund array (quantity/subtotal/line_item) and overwrites whatever the shared normalizer's empty placeholder would have set.
- [x] `ActionsTest.php` — the remaining 9/30 legacy methods (`performPush`/`buildPushPreview`/`validateSaveOrderNoteRequest`) had **no Laravel port at all**; built rather than dropped, since they're real operational recovery actions (manually creating a ShipStation order when auto-sync missed it, correcting a Shopify order note) that the user asked to have built. New: `PushOrderToShipStation`/`SaveOrderNote` (Application layer), `ShipStationClientContract::buildOrderPayload()`/`createOrder()` (pure payload mapper + POST, mirroring legacy's `ShipStation::buildPayload()`/`createOrder()` field-for-field), `ShopifyAdminGateway::updateOrderNote()` (the `orderUpdate` mutation, mirroring legacy's `Shopify::updateOrderNote()` exactly including `userErrors` handling), a new `/orders/push` page (deliberately kept off the existing `orders/lookup`/`orders/spot-check` pages, which are explicitly labelled "Read-only workflow" in the UI), and `RecordPush` (already existed, built but never called — this closes that gap too). The `createOrder` POST is deliberately **not** retried on transient failure (unlike every GET in this app), to avoid double-creating a real ShipStation order; both write endpoints are `can:run-audits`-gated and rate-limited at 5/min. Legacy's tax/shipping-amount fields on the ShipStation payload default to 0 when absent from the order (the shared `findByOrderNumber` query doesn't request `totalTaxSet`/`shippingLines` — extending it would touch every other order-lookup consumer for a rarely-used recovery action, a known, minor accuracy trade-off, not a correctness bug).
- [x] `ShipStationClientTest.php` — auth/lookup/retries/active/awaiting/shipment-date fetch were already covered; `createOrder` closed alongside `ActionsTest.php` above; cache/checkpoint (resumable-pagination-via-disk for long fetches) has zero Laravel usage, same reasoning as `CacheTest.php`.
- [x] `ShopifyClientTest.php` — the last gap, `updateOrderNote`, closed alongside `ActionsTest.php` above.
- [x] `SecurityTest.php` — trusted-proxy CIDR trust and HSTS genuinely cannot be verified in this environment: both depend on the actual production load balancer's IP range and real TLS termination, neither of which exist locally or in CI. Deferred to the production rollout checklist (`laravel-rewrite.md`), not a code gap. Session absolute-timeout has no Laravel equivalent beyond the native idle-based `session.lifetime` — a permanent framework constraint, not something worth a bespoke middleware for. Everything testable (CSP headers, the `oauth` rate limiter) was already covered.
- [x] `ScanRunnerTest.php` — the shared scaffold (credential-check order, date validation, `RunLog` status classification across ok/issues_found/error/validation_error, Slack/email safe-failure dispatch) is replicated per-report across the already-tested `RunXxxReport` classes + `RecordRun`, verified against `RunItemMismatchReport.php` (checks ShipStation credentials before touching Shopify, matching legacy's `needsSS` ordering). Two decisions are deliberately narrower in the rewrite: (1) legacy's `?prefix_history=date` in-place snapshot replay is replaced by a dedicated Saved Reports page (`SavedReportController`) rather than a hidden query-param branch on every report screen, and (2) legacy calls `AuditSnapshot::save()` for every scan tool's full result; only `run_audit` persists a full snapshot in Laravel (verified — `grep` for `auditSnapshots()->updateOrCreate` returns exactly one caller), other tools only log run metadata. A smaller, deliberate persistence footprint, not a gap: `resultRowCount()`'s multi-shape duck-typing (`rows`/`matches`/`pairs`) is likewise replaced by each report computing its own `rows_found` explicitly.
- [x] `ViewHelpersTest.php` — all 26 legacy functions are pure presentation (HTML-string badge/table/link builders) or the manual `esc()` escaping helper; Blade's `{{ }}` auto-escaping replaces `esc()` with a stronger, framework-enforced default (impossible to forget, unlike a manual helper call), and every badge/table/link is built inline with Tailwind directly in each Blade view instead of through shared PHP string-builder functions — the same "no shared reusable class" pattern already used for `GraphQL/ProductNormalizerTest.php`/`GraphQL/OrderFetcherTest.php`. Spot-checked `riskBadge()`: purely presentational (maps a precomputed level string to a CSS class); the actual score/level computation it displays is `RiskScorerTest.php`, already closed.
- [x] `PageLoaderTest.php` — this is specifically the `run_audit` page loader (validation/config errors, Slack/email safe-failure isolation, ignored-orders filtering, dashboard trend/totals aggregation across saved reports), fully covered by `RunAuditControllerTest.php` and `DashboardControllerTest.php`. Laravel's notification safe-failure guarantee is structurally stronger than legacy's try/catch: `AuditSlackNotification`/`ReportEmailNotification` are `ShouldQueue`, so a failed delivery can never affect the request that triggered the audit at all — it fails and retries entirely in the queue worker, out-of-band.
- [x] `SearchLookupPageLoaderTest.php` — 19/19 global search and lookup loader contracts.
- [x] `PackingSlipPageLoaderTest.php` — 6/6 legacy paths.
- [x] `TrackingFeedTest.php` — 7/7 builder contracts.
- [x] `GraphQL/OrderTagInsightsTest.php` — 8/8 tag search/statistics contracts.
- [x] `ProductCatalogueChecksTest.php` — 16/16 catalogue decisions.
- [x] `CatalogQualityTest.php` — 6/6 quality decisions.
- [x] `GiftCardPageLoaderTest.php` — 4/4 visible workflow/failure paths.
- [x] `GiftCardsTest.php` — 8/8 gift-card decisions.
- [x] `InventoryForecastTest.php` — 9/9 forecast decisions.
- [x] `ZombieProductsTest.php` — 7/7 zombie-product decisions.
- [x] `TaxAuditTest.php` — 6/6 zero-tax, exemption, minimum and sorting decisions.
- [x] `ConsentAuditTest.php` — 4/4 email/SMS consent, unknown and sorting decisions.
- [x] `FraudRiskReportTest.php` — 4/4 filtering, signal breakdown, Shopify risk and sorting decisions.
- [x] `AddressCheckTest.php` — 20/20 required fields, postal formats, province, PO Box, carrier and malformed-value decisions.
- [x] `AddressScannerPageTest.php` — 4/4 severity sorting, PO Box filter and clean-address decisions.
- [x] `SameIpTest.php` — 5/5 exact-IP grouping, distinct-email deduplication, exclusions and sorting decisions.
- [x] `OrderPolicyChecksTest.php` — 16/16 Discount Abuse и Tag Policy configuration, required/forbidden semantics и tag normalization decisions.
- [x] `DisputesPageLoaderTest.php` — 7/7 deadline computation, urgency sorting, initial/configuration and Shopify success paths.
- [x] `RepeatRefundsTest.php` — 8/8 threshold, successful-transaction totals, identity grouping and sorting decisions.
- [x] `RefundsTrackerTest.php` — 10/10 missing/active/complete ShipStation status, refund subtotal/fallback, exact number matching and risk sorting decisions.
- [x] `ReturnRmaTrackerTest.php` — 7/7 per-refund rows, notes, item details, SKU aggregation/exclusions and newest-first sorting decisions.
- [x] `ReturnedItemsReportTest.php` — 10/10 refund-date filtering, old-order inclusion, product quantity aggregation, CSV columns/formula safety and escaped output decisions.
- [x] `ApiHealthTest.php` — 8/8 Shopify/ShipStation configuration, live request, scope, returned-version and safe failure decisions; persisted flow health остава отделен capability.
- [x] `AddressChangesTest.php` — 4/4 placement-to-change delay, negative/missing timestamp clamping and current-address output decisions.
- [x] `GoogleAuthFlowTest.php` — OAuth redirect/callback/state handling is delegated to Socialite; cancellation, provider failure, domain policy, existing-user linking, session rotation and throttled routes are covered.
- [x] `GoogleAuthTest.php` — the custom OIDC/PKCE HTTP client is replaced by Socialite; configuration, verified `hd` claims, domain parsing, identity binding and safe failures are covered without persisting provider tokens.
- [x] `BundleCheckPageTest.php` — 9/9 bundle companion and exclusion decisions.
- [x] `CarrierPerfTest.php` — 8/8 carrier grouping, delivery and late-boundary decisions.
- [x] `FulfillmentLogisticsChecksTest.php` — 17/17 SLA and shipment-aging decisions.
- [x] `ItemizedFulfillmentReportTest.php` — 21/21 fulfillment item/date/grouping decisions.
- [x] `OnHoldStallTest.php` — 5/5 wait, hold detail and sorting decisions.
- [x] `PartialFulfillStallsTest.php` — 6/6 partial stall and remaining-item decisions.
- [x] `PostShipAddrChangeTest.php` — 5/5 post-ship timing and sorting decisions.
- [x] `VoidedShipmentsTest.php` — 5/5 voided shipment and nullable-address decisions.
- [x] `GraphQL/CatalogAndFulfillmentTest.php` — 5/5 inclusive on-hold date-filter decisions.
- [x] `OrphanDetectorTest.php` — 7/7 plain, compound, addon-prefixed, empty and sorted orphan decisions.
- [x] `ActiveSsConflictsTest.php` — 6/6 dedupe, refund/cancellation, active matching and sorting decisions.
- [x] `SsShippedUnfulfilledTest.php` — 6/6 shipped counting, match/exclusion, partial state and sorting decisions.

## Как се обновява

1. Преди нов workflow се отварят всички свързани редове и legacy test methods.
2. В PR/slice описанието се записва mapping: legacy method → Laravel test.
3. При частично покрит файл редът остава unchecked и „Какво остава“ се свива.
4. При пълна сверка редът се мести в „Напълно сверени“, а числата горе и в
   `laravel-rewrite.md` се обновяват.
5. След всеки slice се пуска целият Laravel suite; baseline-ът се актуализира
   само при успешно пълно изпълнение.
