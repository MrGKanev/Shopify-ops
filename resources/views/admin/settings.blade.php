@extends('layouts.app')
@section('content')
<div class="flex flex-col gap-6"><section><p class="text-sm font-medium text-indigo-600">Administration</p><h1 class="mt-1 text-3xl font-bold">Settings</h1><p class="mt-2 text-slate-500">Configuration overview for {{ $store->label }}.</p></section><div class="grid gap-5 lg:grid-cols-2"><section class="rounded-xl border p-5"><div class="flex items-center justify-between"><h2 class="text-xl font-bold">Connections</h2><a class="text-sm font-semibold text-indigo-600" href="{{ route('admin.api-health') }}">Test connections</a></div>@foreach($connections as $name=>$configured)<div class="mt-4 flex justify-between"><span>{{ $name }}</span><span class="rounded-full px-3 py-1 text-xs font-semibold {{ $configured ? 'bg-emerald-100 text-emerald-800' : 'bg-amber-100 text-amber-800' }}">{{ $configured ? 'Configured' : 'Needs configuration' }}</span></div>@endforeach<a class="mt-5 inline-block text-sm font-semibold text-indigo-600" href="{{ route('admin.stores.edit',$store) }}">Edit store credentials</a></section><section class="rounded-xl border p-5"><h2 class="text-xl font-bold">Notifications</h2>@foreach($notifications as $name=>$channel)<div class="mt-4 flex items-center justify-between"><div><p>{{ $name }}</p><p class="text-xs text-slate-500">{{ $channel['rules'] }} active rules</p></div><span class="rounded-full px-3 py-1 text-xs font-semibold {{ $channel['configured'] ? 'bg-emerald-100 text-emerald-800' : 'bg-amber-100 text-amber-800' }}">{{ $channel['configured'] ? 'Configured' : 'Needs configuration' }}</span></div>@endforeach<div class="mt-5 flex gap-4 text-sm font-semibold text-indigo-600"><a href="{{ route('admin.slack-rules.edit') }}">Slack rules</a><a href="{{ route('admin.email-rules.edit') }}">Email rules</a></div></section></div><section class="rounded-xl border p-5"><h2 class="text-xl font-bold">Security</h2><p class="mt-2 text-sm text-slate-500">Login abuse is limited automatically by Laravel's per-email and per-IP throttle; there is no persistent IP ban list to maintain.</p><a class="mt-3 inline-block text-sm font-semibold text-indigo-600" href="{{ route('admin.action-log') }}">View administration activity</a></section></div>
@endsection
